# 작업일지 — 2026-03-17

## 요약

판례 파인더 시스템 v3 구현 완료 (Step 1~8). 법제처 판례 204건 수집 및 RAG 질의 시스템 동작 확인.

---

## 완료 작업

### 1. 구현 계획서 작성 (`docs/implementation-plan-v3.md`)
- 9단계 구현 계획 수립
- 사법정보공개포털 크롤러, PDF 수집, SQLite DB, RAG 파이프라인, Streamlit UI, 배포
- Streamlit 웹 UI로 방향 확정 (Next.js/Supabase 대신)

### 2. Step 1~8 구현 완료

| Step | 작업 | 파일 |
|------|------|------|
| 1 | 사법정보공개포털 크롤러 리팩토링 | `crawlers/court_viewer.py` (법고을 + wcd 통합) |
| 2 | PDF 수집 파이프라인 | `crawlers/pdf_collector.py` (CDP print-to-pdf + pdfplumber) |
| 3 | SQLite DB 구축 | `db/store.py` (PrecedentStore, precedents/statutes 테이블) |
| 4 | 기존 데이터 마이그레이션 | `cli.py` (JSON→DB, CLI를 DB 기반으로 전환) |
| 5 | 텍스트 청킹 | `rag/chunker.py` (섹션별 분할, 800자 청크) |
| 6 | 임베딩 & 벡터 DB | `rag/retriever.py` (ChromaDB + OpenAI 임베딩) |
| 7 | 질의응답 엔진 | `rag/qa.py` (OpenAI gpt-4o-mini, 스트리밍) |
| 8 | Streamlit 웹 UI | `app.py`, `pages/data.py` (채팅 + 데이터 관리) |

### 3. 판례 대량 크롤링
- 키워드 9개: 학원, 교습소, 학원법위반, 무등록학원, 과외교습, 교육청, 교육서비스, 유아, 초등
- 키워드당 최대 50건, 3페이지
- **결과: 204건 수집** (기존 10건 + 신규 194건)
- 법원 분포: 대법원(48), 고등법원(28), 지방법원 다수, 전국 30+개 법원

### 4. 벡터 DB 인덱싱
- 204건 판례 → 2,257개 청크
- OpenAI text-embedding-3-small 임베딩
- ChromaDB 저장 완료

### 5. RAG 질의 테스트
- "무등록 학원 운영 시 처벌은?" → 관련 판례 5건 검색, 답변 생성 성공
- LLM/임베딩 모두 OpenAI로 고정

### 6. 기타
- `.env` 파일 생성 (OPENAI_API_KEY 설정)
- `.gitignore` 업데이트 (.env, .streamlit/secrets.toml, DB, chroma_db, pdfs)
- SQLite `check_same_thread=False` 수정 (Streamlit 스레드 이슈)
- `docs/development-log.md` 작성

---

## 미완료 / 블로커

### 법령 조문 크롤링 실패
- 법제처(law.go.kr) headless Chrome 완전 차단
- 직접 API 호출도 IP 인증 실패
- **대기**: 법제처 Open API 승인 후 법령 + 판례 모두 API로 수집 전환

### Step 9: 대법원 개방형 API
- `openapi.scourt.go.kr` API KEY 승인 대기 중

### 법제처 Open API
- 신청 페이지: `https://open.law.go.kr/LSO/openApi/guideList.do`
- 판례 목록/본문 조회 + 법령 조회 API 제공
- 서버 IP 등록 필요

---

## 현재 상태

| 항목 | 값 |
|------|-----|
| 판례 | 204건 |
| 벡터 DB 청크 | 2,257개 |
| 법령 조문 | 0건 (API 승인 대기) |
| PDF | 0건 |
| Streamlit | 로컬 동작 확인 |
| LLM | OpenAI gpt-4o-mini |
| 임베딩 | OpenAI text-embedding-3-small |

---

## 다음 작업

1. 법제처 Open API 승인 → 법령 조문 수집 + 판례 추가 수집
2. 대법원 API 승인 → 하급심 판례 수집
3. Streamlit Cloud 배포
4. 벡터 DB 재인덱싱 (법령 추가 후)
